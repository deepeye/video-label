interface BoxLayerProps {
  videoWidth: number;
  videoHeight: number;
  currentTimeMs: number;
  scale: number;
}

// 占位 — Task 2.7 实现
export function BoxLayer(_props: BoxLayerProps) {
  return null;
}
